version: "2"
sql:
  - engine: "sqlite"
    queries: "query/"
    schema: "schema/"
    gen:
      go:
        package: "models"
        out: "models"
        emit_interface: true
        emit_json_tags: true
